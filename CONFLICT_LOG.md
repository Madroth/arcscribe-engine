# Conflict Log

Detected data conflicts during inbox processing. Each entry labeled `simple` (queued to `DISCOVERY.md` for next session) or `complex` (execution paused until resolved at desktop).

## Entry Format

```
## YYYY-MM-DD HH:MM — <label> — <one-line summary>
- Inbox source: /inbox/<file>.json
- Conflict: <description>
- Affected files: <list>
- Resolution: <pending | resolved by ... on ...>
```

---

*(No conflicts logged yet.)*
