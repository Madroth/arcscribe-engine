# /tests

Test specifications written by Claude Code after each feature, in a format another developer or AI can validate.

Conventions:
- One markdown file per feature: `<feature-slug>.md`.
- Each file lists testable behaviors with pass/fail criteria.
- Tests describe *what* to verify, not *how* — implementation language is decided per feature.
